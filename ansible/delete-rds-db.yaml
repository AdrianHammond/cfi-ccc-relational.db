---
- name: "Delete RDS instance"
  hosts: localhost
  vars: 
    DB_ID: "CFI-validator-db-5"
    
  tasks:
  - name: Delete a DB instance in RDS/AWS in the default VPC
    community.aws.rds_instance:
      id: "{{ DB_ID }}"
      state: absent
      skip_final_snapshot: True

    register: dbdetails

  - name: Print the details of the database
    debug:
      var: dbdetails